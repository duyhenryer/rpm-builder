name: Build RPM Package

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  GO_VERSION: '1.21'

jobs:
  build:
    name: Build RPM
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker image
      run: |
        docker build -t rpm-builder:latest .
        
    - name: Build RPM
      run: |
        chmod +x scripts/build.sh
        ./scripts/build.sh
        
    - name: List built RPMs
      run: |
        echo "📦 Built RPMs:"
        ls -la dist/*.rpm
        
    - name: Upload RPM artifacts
      uses: actions/upload-artifact@v4
      with:
        name: rpm-packages
        path: dist/*.rpm
        retention-days: 5

  # release:
  #   name: Create Release
  #   runs-on: ubuntu-latest
  #   needs: [build, test]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
      
  #   - name: Download RPM artifacts
  #     uses: actions/download-artifact@v3
  #     with:
  #       name: rpm-packages
  #       path: dist/
        
    # - name: Create Release
    #   uses: softprops/action-gh-release@v1
    #   with:
    #     files: dist/*.rpm
    #     tag_name: v${{ github.run_number }}
    #     name: Release v${{ github.run_number }}
    #     body: |
    #       ## RPM Package
          
    #       This release contains the Micro Platform RPM package:
          
    #       - **user-api**: User management API (port 8080)
    #       - **checkout-api**: Checkout API (port 8081)
    #       - **nginx**: Reverse proxy configuration (port 80)
          
    #       ### Installation
          
    #       ```bash
    #       # Install the package (auto-installs nginx)
    #       dnf install micro-platform-*.rpm
          
    #       # Start services
    #       systemctl start micro-platform-all.target
          
    #       # Access the application
    #       curl http://localhost/
    #       curl http://localhost/user/
    #       curl http://localhost/checkout/
    #       curl http://localhost/health
    #       ```
          
    #       ### Features
          
    #       - ✅ Multiple Go HTTP servers with health checks
    #       - ✅ Nginx reverse proxy configuration
    #       - ✅ Systemd target orchestration
    #       - ✅ Simple installation and management
    #       - ✅ Production-ready configuration
    #     draft: false
    #     prerelease: false
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}